// Host side: no-op. All behavior lives in the browser client entry
// (lib/client.js); the host row exists only so the bundle patch resolves.
export const name = 'dsh-conn-dot'
export const inject = []
export function apply() {}
